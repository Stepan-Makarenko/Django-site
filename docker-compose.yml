version: '3.6'

services:
  db:
    image: postgres:10.1-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
  web:
    build: .
    command: python /makarenko_blog/manage.py runserver 0.0.0.0:8080
    ports:
      - 8080:8080
    depends_on:
      - db

volumes:
  postgres_data:
